# A minimal event for the asimov-lalinference end-to-end test. The production
# under test runs against LALInference's own built-in simulated ("fake-cache")
# Gaussian noise, so this doesn't need real GWOSC metadata or strain data.
kind: event
name: FAKE_LALINFERENCE_TEST_EVENT
interferometers:
- H1
